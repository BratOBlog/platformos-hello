<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta content="authenticity_token" name="csrf-param">
    <meta content="{{ form_authenticity_token }}" name="csrf-token">

    <title>Hello world! | platformOS</title>

    <link rel="stylesheet" href="{{ 'styles/output.css' | asset_url }}">
  </head>

  <body class="flex flex-column items-center justify-center gap-8">

    {{ content_for_layout }}


    <script src="{{ 'scripts/app.js' | asset_url }}"></script>

  </body>
</html>